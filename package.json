{
  "name": "nahan-path",
  "version": "0.3.0",
  "description": "Path middleware for nahan",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html mocha",
    "coveralls": "nyc report --reporter=text-lcov | coveralls"
  },
  "repository": "nahanjs/nahan-path",
  "devDependencies": {
    "chai": "^4.2.0",
    "coveralls": "^3.1.0",
    "mocha": "^7.2.0",
    "nahan-context": "0.0.1",
    "nahan-onion": "^0.1.1",
    "nyc": "^15.1.0",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "path-to-regexp": "^6.1.0"
  }
}
